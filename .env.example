# URL of the Pass Explorer app (where Sign in lands).
# In local dev set to http://localhost:3001
# In prod the marketing site (passexplorer.com) sends users to the
# app subdomain.
NEXT_PUBLIC_APP_URL=https://app.passexplorer.com
